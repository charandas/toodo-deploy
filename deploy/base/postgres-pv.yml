apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  labels:
    type: hostPath
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/vda1/pv-vol"
